# Configuración de Base de Datos
# Cambiar esta URL por tu conexión a MongoDB
MONGODB_URI=mongodb://localhost:27017/registro_usuarios

# Puerto del servidor
PORT=3000

# Entorno
NODE_ENV=development

# Configuración de MongoDB Atlas (opcional)
# MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/registro_usuarios

# Para usar este archivo, instala dotenv:
# npm install dotenv
# 
# Y luego modifica backend/index.js para incluir:
# require('dotenv').config();
# 
# Y usa process.env.MONGODB_URI en lugar de la URL hardcodeada
